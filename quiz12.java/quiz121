public class quiz121 {
    public static void main(String[] args) {
		final int CNT = 5;
		//initialized values
        int minval = 0;
        int minidx = 0;
        int tmp = 0;

		Scanner scnr = new Scanner(System.in);
		int N = scnr.nextInt();
		int[] numbers = new int[N];
		//read original values
        for (int i = 0; i < N; i++)
			numbers[i] = scnr.nextInt();
        //update the starting point/index of for loop (how? which for-loop will do this?)
        int bounds = numbers.length;
    
        for (int i = 0; i < bounds-1; i++){
            minidx = i;
            for (int j = 0; j < /*what value??*/; j++){
                if (numbers[j]</*oh god what value*/){
                    minidx = j;
                }
            }
        }
        //create a new array for sorted values

            //find the smallest value, assign it's index and value to seperate variables
            
            //stop after five sorts using CNT (so we count using a for-loop too)
            //will need a for-loop to print as well

        for (int i = 0; i < numbers.length; i++) {
            minidx = i;
            for (int j = i + 1; j < /*what value?*/; j++)
        }

		

		scnr.close();
	}

}